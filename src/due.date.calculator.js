// due.date.calculator.js

'use strict';

var calc = {};

calc.calculateDueDate = function(submitDate, turnaroundTime, timeFrames){
  if (turnaroundTime === 0) return submitDate;
};

module.exports = calc;
